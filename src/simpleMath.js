function square(number){
    console.log(`square function called with argument ${number}`)
    return (number*number);
}

function cube(number){
    console.log(`cube function called with argument ${number}`)
    return (number*number*number);
}